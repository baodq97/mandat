# build output + repo-local tools (golangci-lint binary, mandat binary)
/bin/
/dist/

# test artifacts
*.test
/coverage.out

# per-machine gate sensor data (feeds distill locally, never shared state)
/.govkit/
